# 简历大师 · 运营与变现执行手册

线上产品：https://resume-master.app.workbuddy.host/
会员卡密：见同目录 `codes.txt`（已生成 300 个，格式 RM-XXXX-XXXX），校验逻辑在 `index.html` 的 `redeemFromInput()`（FNV-1a + SECRET，%97===0 才解锁）。

## 一、变现闭环（已上线，两条可验证路径）

> 关键原则：**纯前端无法验证「付款」和「真实分享」，所以这两类免费/白嫖解锁已全部移除**。
> 会员只通过「闲鱼购买的卡密」这一条能真实验证的路径解锁——用户必须先付款拿到卡密，回来输入，前端校验通过才解锁。

### 路径 A：闲鱼卡密（核心付费路径）
1. 用户点「升级会员」→ 弹窗 → 点「去闲鱼购买激活码」跳转到 `CARD_SELL_URL`（已设为用户发布的闲鱼「简历大师激活码」商品）。
2. 在闲鱼付款 ¥9.9 → 卖家（你）把 `codes.txt` 里的一个卡密（RM-XXXX-XXXX）发给买家。
3. 用户回到页面输入卡密 → `redeemFromInput()` 校验 → 通过即解锁全部 8 套模板 + 去水印。
4. 你只需：① 在闲鱼挂好「简历大师激活码」商品（已发布）；② 每成交一单，从 `codes.txt` 取一个未用过的码发给买家即可；③ 链接已填好，无需改代码。

> 安全说明：卡密校验是纯前端的，专业破解者可能自己造码。对 ¥9.9 客单价足够（靠便利而非防盗），正式规模化建议接后端校验——到时告诉我即可加。
> 换码/批量生成：改 `generate_codes.js` 的 `COUNT` 后重跑即可。

### 路径 B：A-ADS 展示广告（自主变现「其他路径」，流量即钱）
- **这是「有流量就能立刻收钱」最直接的一根管子**：不依赖任何付费/转化动作，页面有真实访客，A-ADS 就按展示（unique impression）给 BTC。
- 全站每页已埋好广告坑位（首页 3 个：顶部/中部/页脚；内页 2 个：顶部/页脚），统一由 `ads.js` 的 `AD_ZONES = ["", "", ""]` 数组驱动。
- 开通只需 2 分钟、之后全自动：
  1. 打开 a-ads.com 注册发布者（无需邮箱/KYC，可匿名）；
  2. 收款地址填钱包 `0x37659ff58adc803cbbf45c4923d2f6169add2519`（A-ADS 支持 ETH）；
  3. 建 1~3 个广告位（zone），尺寸建议 728x90 / 300x250 / 320x100 / 自适应；
  4. 把每个 zone id 依次填进 `ads.js` 的 `AD_ZONES` 数组，重新部署即可。
- 填几个坑位就显示几个广告（A-ADS 每页最多 3 个）；零审核、无最低流量要求，前 1-2 天展示偏少属正常。
- 这条路径与会员卡密互补：会员是「转化型收入」，广告是「展示型收入」——只要有人来逛，就有钱进。

## 二、引流文案（复制即用）

### 小红书（带图：截简历成品图）
标题：校招简历不会写？这个免费工具3分钟出图
正文：
秋招季简历傻眼的姐妹看过来
自己做了个在线简历生成器，亲测好用：
8套高质感模板（现代/经典/商务/极简/紧凑/创意/国企/清新）
填完实时预览，一键导出PDF
完全免费，不套路
免费版带轻水印，想要无水印+全部高级模板的有9.9会员（闲鱼激活码即开）
链接放评论区了，自取
#简历模板 #校招 #秋招 #简历怎么写 #免费工具

### 知乎（回答「如何写一份好简历」时贴）
写简历最难的不是内容，是排版。分享一个我做的在线工具：填信息实时出 PDF，8 套模板覆盖互联网/国企/设计不同风格，免费可用。需要无水印和全部高级模板有会员选项（闲鱼激活码解锁）。链接：resume-master.app.workbuddy.host

### 豆瓣找工作小组 / 应届生求职群（短贴）
【免费】在线简历生成器，3分钟导出PDF，8套模板，不收钱。要无水印全部高级模板有9.9会员。地址：resume-master.app.workbuddy.host 自取，帮转需注明出处。

## 三、发布节奏（建议）
- 每周 3 条：小红书 2（不同模板截图）+ 知乎 1（回答长尾问题）
- 每日：在 2-3 个求职微信群/QQ群 发一次短贴（别刷屏，隔天发）
- 评论区置顶链接 + 一句「会员激活码在闲鱼购买」

## 四、怎么看访问量（已接入）
- 访问统计：已移除。原不蒜子（busuanzi）因本站域名长度超其 22 字符上限被禁用，且当前环境无可用建表工具、无可自托管的统计后端，故 footer 计数整块删除，仅保留版权与隐私说明。后续若需真实 PV/UV，可接第三方统计（51.la 等，无域名长度限制）。
- 查看方法：打开 https://resume-master.app.workbuddy.host/ 拉到页面最底部即可看到数字；任何页面都显示同一全站累计值。
- 优点：免注册、国内可用、零成本；缺点：只有 PV/UV 总数，没有来源/设备/跳出等明细。
- 想要更细的（来源渠道、搜索词、设备、转化率）：需要你开一个免费统计账号（Umami / Plausible / 百度统计 / Google Analytics 均可），把统计 ID 给我，我接进页面即可在对应后台看完整报表。
- 会员付款数：在闲鱼订单里看即可（前端无法读取支付结果，这是移除扫码直付的根因）。

## 五、下一步可让我做的（说一声就干）
- 接闲鱼/后端 API 自动校验卡密（防作弊、可规模化）
- 增加行业模板 / 导出 Word / 微信长图
- 接后端校验 + 自动发卡
- 做微信/抖音小程序版（收益更高）
- 百度统计 / IndexNow 已接入，可继续扩充内容页吃 SEO 长尾

## 六、SEO 自然流量层（已上线，无需你发帖）
为覆盖「简历怎么写 / 校招简历 / 程序员简历 / ATS 简历」等长尾搜索词，已部署内容页并互相链接：
- /guide.html：简历写作完全指南
- /campus.html：校招简历模板
- /developer.html：程序员简历
- /ats.html：ATS 友好简历
- /example.html：各行业范文
- /ats-check.html：ATS 检测
- /cover-letter.html：求职信生成器

已配 robots.txt + sitemap.xml + IndexNow（key 已推送）；全站内页脚互链。

## 七、当前状态与诚实边界
- 已就位：8 套模板、6 行业一键套用、AI 诊断、范文/指南/ATS/求职信内容页、会员卡密校验（可验证）、不蒜子统计、SEO、A-ADS 广告位（待填 zone id）。
- 已移除（无法验证，故不做）：微信扫码直付（点「已付」直接发码=白嫖）、分享裂变免费解锁（点分享=白嫖）、?ref= 推荐码自动解锁。
- 唯一无法自主解决：初始流量/分发。如果你坚持不推广，盈利只能依赖 ① SEO 慢慢起量（数周~数月）；② A-ADS 按量计费（也需访问量）。AI 无法替你发朋友圈/小红书/知乎。
- 若你改变主意，只需顺手发一次链接（任何地方），就能打破冷启动；其余后续我全包。
